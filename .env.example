# Optional: when you add a custom domain, set this in Vercel Project → Settings → Environment Variables
# so any future build-time SEO can pick it up. The live app still uses window.location.origin at runtime.
# VITE_SITE_ORIGIN=https://www.yourdomain.com
